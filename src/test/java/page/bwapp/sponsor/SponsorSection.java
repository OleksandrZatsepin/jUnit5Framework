package page.bwapp.sponsor;

import org.openqa.selenium.WebDriver;
import page.MainPage;

public class SponsorSection extends MainPage {
    public SponsorSection(WebDriver driver) {
        super(driver);
    }
}
