package page.bwapp.main;

import org.openqa.selenium.WebDriver;
import page.MainPage;

public class MainSection extends MainPage {
    public MainSection(WebDriver driver) {
        super(driver);
    }
}
