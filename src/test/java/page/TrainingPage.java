package page;

import org.openqa.selenium.WebDriver;

public class TrainingPage extends  MainPage{

    public TrainingPage(WebDriver driver, String baseUrl) {
        super(driver, baseUrl);
    }
}
